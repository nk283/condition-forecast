name: Daily Condition Forecast

on:
  schedule:
    # UTC時刻（日本時刻より-9時間）
    # 6時(JST) = 21時前日(UTC)
    # 14時(JST) = 5時(UTC)
    # 22時(JST) = 13時(UTC)
    - cron: '0 21 * * *'  # 前日21時 = 6時JST
    - cron: '0 5 * * *'   # 5時 = 14時JST
    - cron: '0 13 * * *'  # 13時 = 22時JST
  workflow_dispatch:  # 手動実行用

permissions:
  contents: write
  pages: write

jobs:
  forecast:
    runs-on: ubuntu-latest
    timeout-minutes: 30

    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Setup Node.js 18
        uses: actions/setup-node@v4
        with:
          node-version: '18'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Run condition forecast
        env:
          OPENWEATHER_API_KEY: ${{ secrets.OPENWEATHER_API_KEY }}
          WEATHER_LAT: ${{ secrets.WEATHER_LAT }}
          WEATHER_LON: ${{ secrets.WEATHER_LON }}
          GOOGLE_CLIENT_ID: ${{ secrets.GOOGLE_CLIENT_ID }}
          GOOGLE_CLIENT_SECRET: ${{ secrets.GOOGLE_CLIENT_SECRET }}
          GOOGLE_TOKEN: ${{ secrets.GOOGLE_TOKEN }}
          NODE_ENV: production
        run: npm start

      - name: Upload dashboard artifact
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: forecast-dashboard-${{ github.run_id }}
          path: |
            dashboard_72h.html
            data/hourly_scores.json
          retention-days: 90

      - name: Enable GitHub Pages (REST API)
        if: success()
        run: |
          REPO_OWNER="${{ github.repository_owner }}"
          REPO_NAME="${{ github.event.repository.name }}"

          echo "📄 GitHub Pages を有効化中..."
          curl -X POST \
            -H "Accept: application/vnd.github+json" \
            -H "Authorization: Bearer ${{ secrets.GITHUB_TOKEN }}" \
            "https://api.github.com/repos/${{ github.repository }}/pages" \
            -d '{"source":{"branch":"main","path":"/"}}' \
            --silent --show-error \
            || echo "✓ GitHub Pages already enabled or will be configured"

          echo ""
          echo "📊 ダッシュボード URL:"
          echo "https://${REPO_OWNER}.github.io/${REPO_NAME}/dashboard_72h.html"

      - name: Deploy to GitHub Pages
        if: success()
        run: |
          git config user.name "GitHub Actions"
          git config user.email "actions@github.com"

          # ダッシュボードとデータファイルをステージング
          git add dashboard_72h.html data/hourly_scores.json

          # 変更があればコミット
          if ! git diff --cached --quiet; then
            git commit -m "chore: update forecast dashboard $(date '+%Y-%m-%d %H:%M:%S')"
            git push origin main
            echo "✅ Dashboard deployed to GitHub Pages"
          else
            echo "ℹ️  No changes to commit"
          fi

      - name: Send Slack notification (Success)
        if: success()
        uses: slackapi/slack-github-action@v1.24.0
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          payload: |
            {
              "text": "✅ 体調予報を更新しました",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "✅ 体調予報更新完了",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*実行時刻*\n${{ github.event.head_commit.timestamp }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*ステータス*\n✅ 成功"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*🔗 リンク*\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|実行ログを見る> | <https://nk283.github.io/condition-forecast/dashboard_72h.html|📊 ダッシュボード>"
                  }
                }
              ]
            }

      - name: Send Slack notification (Failure)
        if: failure()
        uses: slackapi/slack-github-action@v1.24.0
        with:
          webhook-url: ${{ secrets.SLACK_WEBHOOK_URL }}
          payload: |
            {
              "text": "❌ 体調予報の実行に失敗しました",
              "blocks": [
                {
                  "type": "header",
                  "text": {
                    "type": "plain_text",
                    "text": "❌ エラー発生",
                    "emoji": true
                  }
                },
                {
                  "type": "section",
                  "fields": [
                    {
                      "type": "mrkdwn",
                      "text": "*実行時刻*\n${{ github.event.head_commit.timestamp }}"
                    },
                    {
                      "type": "mrkdwn",
                      "text": "*ステータス*\n❌ 失敗"
                    }
                  ]
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*エラー内容*\n体調予報の実行に失敗しました。詳細はログを確認してください。"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "*🔗 ログ確認*\n<${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}|詳細ログを見る>"
                  }
                }
              ]
            }
