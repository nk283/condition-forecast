# システムアーキテクチャ

## 🏗️ 全体構成図

```
┌─────────────────────────────────────────────────────────────────┐
│                     体調予報システム                           │
└─────────────────────────────────────────────────────────────────┘

┌──────────────────────┐    ┌──────────────────────┐
│   カレンダーデータ    │    │   気象データ取得      │
│  Google Calendar API │    │ OpenWeather API      │
└──────────┬───────────┘    └──────────┬───────────┘
           │                           │
           │                           │
           ▼                           ▼
    ┌──────────────────────────────────────────┐
    │      データ収集・正規化層               │
    ├──────────────────────────────────────────┤
    │  CalendarService   │   WeatherService    │
    └──────────┬──────────────────────┬───────┘
               │                      │
               └──────────┬───────────┘
                          │
                          ▼
    ┌──────────────────────────────────────────┐
    │     体調スコア計算エンジン               │
    ├──────────────────────────────────────────┤
    │  ConditionScoreEngine                    │
    │  ・気温スコア計算                        │
    │  ・湿度スコア計算                        │
    │  ・日照スコア計算                        │
    │  ・空気質スコア計算                      │
    │  ・気圧スコア計算                        │
    │  ・スケジュールスコア計算                │
    │  ・加重合計 → 総合スコア                │
    └──────────┬───────────────────────────────┘
               │
               ▼
    ┌──────────────────────────────────────────┐
    │      レポート生成・出力層               │
    ├──────────────────────────────────────────┤
    │  ReportGenerator                         │
    │  ・テキスト形式レポート                  │
    │  ・JSON形式レポート                      │
    │  ・詳細な要因分析                        │
    │  ・推奨事項生成                          │
    └──────────┬───────────────────────────────┘
               │
    ┌──────────┴──────────────┬────────────┐
    ▼                         ▼            ▼
  コンソール出力           JSON ファイル   Web API
```

---

## 🔄 データフロー

### 1. **データ取得フェーズ**

```
┌─────────────────────────────────────────┐
│ 気象データ取得（WeatherService）         │
└────┬────────────────────────────────────┘
     │
     ├─ API リクエスト → OpenWeather
     ├─ レスポンス解析
     └─ 正規化
         ├ temperature: -5 to 40℃
         ├ humidity: 0-100%
         ├ pressure: 900-1050 hPa
         ├ daylight: 0-12 hours
         └ weather_description: string

┌─────────────────────────────────────────┐
│ カレンダーデータ取得（CalendarService）  │
└────┬────────────────────────────────────┘
     │
     ├─ OAuth2 認証
     ├─ イベント取得（指定日付）
     ├─ テキスト解析（キーワード検出）
     └─ 構造化
         ├ hasMeetings: boolean
         ├ hasOutdoorActivities: boolean
         ├ sleepInterruption: boolean
         ├ mealInterruption: boolean
         └ eventCount: number
```

### 2. **スコア計算フェーズ**

```
正規化されたデータ
     │
     ├─ calculateTemperatureScore()
     │   ├─ 最適温度範囲チェック
     │   ├─ 快適温度範囲チェック
     │   └─ スコア算出（0-100）
     │
     ├─ calculateHumidityScore()
     │   ├─ 気温との相互作用計算
     │   ├─ ペナルティ係数決定
     │   └─ スコア算出（0-100）
     │
     ├─ calculateIlluminationScore()
     │   ├─ 時刻チェック（昼間のみ）
     │   ├─ 日照時間に応じた評価
     │   └─ スコア算出（0-100）
     │
     ├─ calculateAirQualityScore()
     │   ├─ 屋外予定判定
     │   ├─ AQI レベル判定
     │   └─ スコア算出（0-100）
     │
     ├─ calculatePressureScore()
     │   ├─ 標準気圧との差分計算
     │   ├─ ペナルティ適用
     │   └─ スコア算出（0-100）
     │
     └─ calculateScheduleScore()
         ├─ 会合予定による減点
         ├─ 外出予定による減点
         ├─ 睡眠阻害による減点
         ├─ 食事阻害による減点
         └─ スコア算出（10-100）

     │
     ▼
各因子スコア: {
  temperature: 85,
  humidity: 90,
  illumination: 100,
  airQuality: 95,
  pressure: 80,
  schedule: 75
}

     │
     ▼
加重合計計算
  = 85×0.25 + 90×0.15 + 100×0.20 + 95×0.15 + 80×0.10 + 75×0.15
  = 21.25 + 13.5 + 20 + 14.25 + 8 + 11.25
  = 88.25 → 総合スコア: 88
```

### 3. **レポート生成フェーズ**

```
総合スコア: 88
     │
     ├─ 評価判定
     │   └─ スコア 80以上 → 「良好」😊
     │
     ├─ テキストレポート生成
     │   ├─ ヘッダー（日時）
     │   ├─ スコア・評価表示
     │   ├─ アドバイス
     │   ├─ 各要因詳細（スコアバー付き）
     │   ├─ 気象情報
     │   ├─ リスク分析
     │   └─ 推奨事項
     │
     └─ JSON レポート生成
         ├─ score (total, evaluation, advice)
         ├─ factorScores (各要因のスコア)
         ├─ weather (気象データ)
         ├─ risks (詳細リスク)
         └─ recommendations (推奨事項)

     │
     ▼
ファイル出力 / コンソール出力 / API返却
```

---

## 📦 クラス設計

### WeatherService

```
WeatherService
├── constructor(apiKey, lat, lon)
├── getCurrentWeather()         → Promise<WeatherData>
├── getForecast()               → Promise<WeatherData[]>
├── getAirQuality()             → Promise<AQI>
├── getAQI()                    → Promise<AQI>
└── formatWeatherData(data)     → WeatherData
```

**WeatherData 構造**:
```javascript
{
  timestamp: Date,
  temperature: number,
  feelsLike: number,
  humidity: number,
  pressure: number,
  windSpeed: number,
  cloudiness: number,
  visibility: number,
  rainVolume: number,
  snowVolume: number,
  uvi: number,
  weatherMain: string,
  weatherDescription: string,
  weatherIcon: string
}
```

### CalendarService

```
CalendarService
├── constructor(clientId, clientSecret, redirectUri)
├── initOAuth2Client()
├── initCalendarClient()
├── getAuthUrl()                → string
├── refreshToken(code)          → Promise<void>
├── getEvents(startDate, endDate) → Promise<Event[]>
├── getEventsForDate(date)      → Promise<Event[]>
└── analyzeSchedule(events)     → ScheduleAnalysis
```

**ScheduleAnalysis 構造**:
```javascript
{
  hasEvents: boolean,
  eventCount: number,
  hasMeetings: boolean,
  hasOutdoorActivities: boolean,
  sleepInterruption: boolean,
  mealInterruption: boolean,
  events: Event[]
}
```

### ConditionScoreEngine

```
ConditionScoreEngine
├── constructor(customWeights?)
├── calculateTemperatureScore(temp)
├── calculateHumidityScore(humidity, temperature)
├── calculateIlluminationScore(daylight, hour)
├── calculateAirQualityScore(aqi, hasOutdoorPlans)
├── calculatePressureScore(pressure)
├── calculateScheduleScore(scheduleAnalysis)
├── calculateTotalScore(data)   → ScoreResult
├── getEvaluation(score)        → Evaluation
└── getDetailedAnalysis(scores, data) → Analysis[]
```

**ScoreResult 構造**:
```javascript
{
  totalScore: number,           // 0-100
  factorScores: {
    temperature: number,
    humidity: number,
    illumination: number,
    airQuality: number,
    pressure: number,
    schedule: number
  },
  evaluation: {
    level: string,              // "良好" | "注意" | "要注意" | "警告"
    emoji: string,              // 😊 | 😐 | 😓 | 😰
    advice: string
  }
}
```

### ReportGenerator

```
ReportGenerator
├── generateReport(result, analysis, weather, date)
│   ├── generateTextReport()    → string
│   └── generateJsonReport()    → object
├── createScoreBar(score)       → string
└── getRecommendations(score, analysis) → string
```

---

## 🔌 API 仕様

### 入力インターフェース

```javascript
// メイン実行
const data = {
  temperature: number,           // -50 to 50℃
  humidity: number,             // 0-100%
  pressure: number,             // 900-1050 hPa
  daylightHours: number,       // 0-12 hours
  aqi: number,                 // 0-500
  hasOutdoorPlans: boolean,
  scheduleAnalysis: {
    hasEvents: boolean,
    eventCount: number,
    hasMeetings: boolean,
    hasOutdoorActivities: boolean,
    sleepInterruption: boolean,
    mealInterruption: boolean,
    events: Array
  }
};

const result = scoreEngine.calculateTotalScore(data);
```

### 出力インターフェース

```javascript
// JSON レスポンス例
{
  "date": "2026-02-10T13:10:39.884Z",
  "score": {
    "total": 88,
    "evaluation": "良好",
    "advice": "体調が良好です。通常通りの活動が推奨されます。"
  },
  "factorScores": {
    "temperature": 100,
    "humidity": 100,
    "illumination": 100,
    "airQuality": 100,
    "pressure": 100,
    "schedule": 75
  },
  "weather": {
    "temperature": 8,
    "feelsLike": 5,
    "humidity": 55,
    "pressure": 1015,
    "description": "晴れ",
    "visibility": 10000,
    "windSpeed": 3.2
  },
  "risks": [],
  "recommendations": "• 通常通りの活動を継続してください\n• 特に制限のない状況です"
}
```

---

## 🗄️ ファイルレイアウト

```
Condition_Forecast/
├── src/
│   ├── index.js                    # エントリーポイント
│   ├── services/
│   │   ├── weatherService.js       # 気象データAPI
│   │   ├── calendarService.js      # Google Calendar API
│   │   └── conditionScoreEngine.js # スコア計算ロジック
│   ├── utils/
│   │   └── reportGenerator.js      # レポート生成
│   └── config/
│       └── (今後の認証設定など)
├── tests/                          # テストスイート（今後）
├── data/                           # データ保存（今後）
│   ├── scores/                     # 過去スコア
│   └── logs/                       # 実行ログ
├── public/                         # Web UI（今後）
├── node_modules/                   # 依存パッケージ
├── .env                           # 環境変数（本番）
├── .env.example                   # サンプル設定
├── .gitignore                     # Git除外設定
├── package.json                   # パッケージ設定
├── package-lock.json              # 依存関係ロック
├── demo.js                        # デモンストレーション
├── README.md                      # 使用方法
├── IMPLEMENTATION_GUIDE.md        # 実装ガイド
└── SYSTEM_ARCHITECTURE.md         # このファイル
```

---

## 🔐 セキュリティ考慮事項

1. **API キー管理**
   - `.env` に保存（.gitignore に登録）
   - 環境変数経由でロード
   - 本番環境では複数の認証層を追加

2. **OAuth 2.0 フロー**
   - Google Calendar 認証時は HTTPS 必須
   - リフレッシュトークンを安全に保管

3. **データプライバシー**
   - ユーザーの予定情報は機密扱い
   - 外部サービスへの送信を最小化
   - ローカルで処理可能な範囲での実装

---

## 📈 スケーラビリティ戦略

### Phase 2: 複数ユーザー対応
- ユーザー管理システム
- データベース統合（MongoDB/PostgreSQL）
- 個別の設定・重み付け管理

### Phase 3: リアルタイム処理
- 定期実行スケジューラ（cron/setTimeout）
- WebSocket による リアルタイム通知
- 予報キャッシング機構

### Phase 4: 機械学習
- 過去データから個別パターン学習
- 予測精度の向上
- 異常検知機能

---

**最終更新**: 2026年2月10日
